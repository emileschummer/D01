
import numpy as np

"""

This functions finds to which (angle) bin a certain frame corresponds to
Evey bin is X degrees wide

"""

# We have 36 bins of 10 degrees each
# Each frame is already assigned to a bin through the binds data file

# Input a frame number and output the angle bin it corresponds to

